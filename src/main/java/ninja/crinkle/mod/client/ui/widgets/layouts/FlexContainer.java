package ninja.crinkle.mod.client.ui.widgets.layouts;

import ninja.crinkle.mod.client.ui.themes.BoxTheme;
import ninja.crinkle.mod.client.ui.themes.Theme;

public class FlexContainer extends Container {
    public FlexContainer(int x, int y, int width, int height, Theme theme, LayoutManager layoutManager,
                         BoxTheme.Type borderThemeType) {
        super(x, y, width, height, theme, layoutManager, borderThemeType);
    }
}
